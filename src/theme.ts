// Palette dell'app — fonte unica: colors.json (lo stesso usato da tailwind.config.js).
// Usare COLORS per gli usi NON-className: SVG, ActivityIndicator, gradienti, grafici.
// Per cambiare palette, modifica SOLO colors.json.
import colors from "../colors.json";

export const COLORS = colors;
